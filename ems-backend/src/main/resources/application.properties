spring.application.name=springbootfirst

# ? Correct MySQL driver and JDBC URL using Railway DB
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://centerbeam.proxy.rlwy.net:53380/railway
spring.datasource.username=root
spring.datasource.password=RlWApposYMAjoMuhfHjNJKIAaABGmIea

# ? Hibernate / JPA settings
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ? Spring Security user (if you're using in-memory authentication)
spring.security.user.name=admin
spring.security.user.password=admin
spring.security.user.roles=ADMIN

# ? JWT settings (used for token signing and expiration)
app.jwt-secret=ThisIsMyVerySecureAndLongJwtSecretUsedOnlyForSigningTokensInSpringBootAndForItsSecurity!
app.jwt-expiration-milliseconds=36000000
